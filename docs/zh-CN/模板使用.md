# 模板使用

## 插件logo自定义

默认的logo存放在`public/icons`目录下，你可以根据需要替换这些图标。

## 插件名称自定义

默认的插件名称为`Vue Chrome Extension Template`，你可以根据需要在`manifest.js`文件中修改。

## 插件描述自定义

默认的插件描述为`Vue Chrome Extension Template`，你可以根据需要在`manifest.js`文件中修改。
